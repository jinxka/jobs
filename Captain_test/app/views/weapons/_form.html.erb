<%= form_with(model: weapon, local: true) do |form| %>
  <% if weapon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weapon.errors.count, "error") %> prohibited this weapon from being saved:</h2>

      <ul>
      <% weapon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :weapon_name %>
  </div>

  <div class="field">
    <%= form.label :dexterity %>
    <%= form.number_field :dexterity, id: :weapon_dexterity %>
  </div>

  <div class="field">
    <%= form.label :strength %>
    <%= form.number_field :strength, id: :weapon_strength %>
  </div>

  <div class="field">
    <%= form.label :intelligence %>
    <%= form.number_field :intelligence, id: :weapon_intelligence %>
  </div>

  <div class="field">
    <%= form.label :damage %>
    <%= form.number_field :damage, id: :weapon_damage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
